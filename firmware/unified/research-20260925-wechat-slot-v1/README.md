# Unattached service-slot candidate — NEVER INSTALL

98 guarded tests pass with 915 content hashes and four tool fingerprints.
`executed-proof/` contains artificial ARM witnesses, repeated objects and
expected strict whole-26 link refusals. No file here is an OTA image.

The real adapter compiles and preserves four other service registrations in
the emulator, but does not close shared-event routing, callback/storage
ownership, advertising retirement or any physical/recovery gate. Full26 still
exceeds capacity by at least1154 bytes before additional binding costs.
See [the exact scope](../../../docs/UNIFIED_WECHAT_RETIREMENT.md).
