# OFF-RING callback / advertising witnesses — NEVER INSTALL

144 guarded tests passed in 12.92 s, no failures/errors/skips. This includes
the previous 98 slot/service/transport tests, 28 new opaque event-gate tests
and 18 advertising-plan tests. No stock file, installed ring, production source
list, admission or release gate changed. The last full main regression remains
the separate 3251-test control-owner checkpoint.

`proof-supplement.json` binds 922 inputs, 69 artifacts, three test reports and
four tools. Its SHA-256 is
`8048944469be3727f3ec78f74672d3bcf5a3a9e83c6893fb4fb357aaf8725f02`.
Independent review verified all 994 content hashes, tools, 144 ordered cases /
432 passing phases, unchanged prior 915 files, instruction edits and whole-27
budget/refusal. A separate focused rerun passed all 46 new cases in 5.97 s.
The guarded launcher is `/tmp/whip-wechat-routing-proof.eW8vOz/run.py`; reproduce
with new output paths, never overwrite this archive. `executed-proof/` retains
the actual test-run objects, stack reports, repeated artificial ELFs, link logs
and nested reports. Nested absolute input paths refer to that run's retained
temporary fixture directories; copied artifact hashes are in the supplement.

Two callback literal roots and one setup BL are changed only in emulator RAM.
The new gate filters the published owned ID without dereferencing its event;
all other IDs including general FF reach the original callback. Callback
bodies, ID lifetime, ROM/RTOS and physical fencing are still fixtures or absent.
The separate advertising witness executes selected stock instructions with
13 size-neutral edits: omit the FEE7 service-list element, preserve opaque
manufacturer data, and submit 12 bytes at both reviewed parameter-0x262 sites.
Source MAC and skipped boot/configuration paths are explicit fixtures.
These witnesses do not execute a fully integrated startup, establish physical
GATT/advertising, close all references or qualify Health/recovery.

Whole 27-object input lower bound: 10706/9520, at least 1186 over before
additional callback constant 12, alignment/unwind and missing bindings.
Five strong symbols remain unresolved. Both strict whole-code links fail
without emitting a production ELF. All artificial ELFs are non-installable.
See [the audit](../../../docs/UNIFIED_WECHAT_RETIREMENT.md) for the scope and
remaining constraints.
