# Generated experiment — NEVER INSTALL

44 guarded tests pass; no accepted firmware source was changed. This archive
contains artificial full32/low16 runtime proof ELFs and an intentionally unsafe
low8 mutant, plus expected whole25 production-link refusals. None is firmware
for a ring. No production bindings, RAM ownership or recovery approval exist.

The variant saves64 persistent RAM bytes but adds8 compiler-local stack bytes
to `wr_next`. Whole25 input lower bound10608/9520 remains1088 over before all
missing costs. Full downstream integration of this private-layout change has
not been executed. See [the detailed checkpoint](../../../docs/UNIFIED_TIMESTAMP_STORAGE.md).

`proof-supplement.json` records the exact44 identities,132 phases and1202
content hashes. `executed-runtime-proof/` holds the separately built binaries
actually exercised by the tests; root-level generated objects establish the
budget independently. `structural-study/` and `retired-descendants/` retain
rejected or hypothetical alternatives, not approved code space.
