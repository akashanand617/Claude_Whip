# OFF-RING evidence only — NEVER INSTALL

No firmware container or production ELF was constructed. The real 25-object
link refuses three missing physical bindings and exceeds the configured append
allowance at input-only lower bound: 10610/9520, at least 1090 bytes over.

`proof-supplement.json` records 258 passing guarded tests, zero skips/failures,
767 content hashes, tools/support fingerprints and exact reproduction command.
Owner/mailbox proof ELFs use artificial storage/link addresses and must never
be transferred to a ring. ROM/physical/source/clock contracts remain fixtures.

- `proof-control-owner/`: actual C owner composition and test-only ABI bindings.
- `proof-control-mailbox/`: two-frame copied-input primitive and contract tests.
- `proof-optimization-study/`: unsuccessful compiler-only size study, not the
  accepted toolchain, an approved fit or physical qualification.
- `failed-link-NOT-FIT.map` and `link-*.json`: genuine production-link refusals.

Main 3251-test regression is separately archived in
`../build-20260925-control-owner-regression-v1/`; its source list excludes the
new owner/mailbox/wait units. See [the checkpoint](../../../docs/UNIFIED_CONTROL_OWNER.md)
for evidence boundaries, RAM accounting and six still-open release gates.
