# Control-ingress provenance counterexamples — no firmware

**107 guarded tests pass in22.686s**, no skips/failures/errors:13 new cases,
32 existing control-owner cases and62 exact-stock transport cases. This is
not a physical callback replay observation or permission to attach a provider.

Hypothetical upstream relabeling executes actual ARM owner/mailbox/dispatcher:
original-generation old Gesture frames are ignored, while relabeled-current
frames trigger software ENTRY. No real optical/source completion is claimed.
Additional tests cover upstream timing loss and exact UART stack-argument reads.
The seventh callback argument remains compatible-header evidence only.

Manifest SHA-256:
`6ceacaf36ff5346eb564fcbeb9bb70fedfb99fefe03c02a6f8427518a54a2e24`.
Independent review verified1134 inputs,41 artifacts,three reports,four tools,
107 ordered identities/321 passing phases, both nested owner reports and repeat
objects/stack reports/artificial ELFs. Prior1130 discovery-checkpoint hashes are
unchanged. The compatible header is archived, not treated as the exact ring SDK.

Launcher: `/tmp/whip-control-provenance-proof.bwS6g6/run.py`; reproduction must
use new archive/report/basetemp paths. The strict owner link still refuses the
three genuine physical bindings; test-only providers occur only in artificial
ELFs. No accepted C/Swift protocol, binary, production hook, device state or
memory allocation changed. Whole28 remains10798/9520 before extra costs, with
six missing bindings. See `docs/UNIFIED_CONTROL_INGRESS.md` for exact limits.
