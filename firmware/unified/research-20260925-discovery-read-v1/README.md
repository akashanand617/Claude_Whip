# OFF-RING discovery read / selected startup — NEVER INSTALL

178 guarded tests passed in 30.93 s, no failures/errors/skips: prior 144 cases,
six new combined-startup cases and 28 discovery-read cases. There is no ring
access, stock file edit, production source-list change or release admission.
The full main regression remains the separate 3251-test owner checkpoint.

`proof-supplement.json` binds 1001 inputs, 126 artifacts, three test reports,
four tools and 178 ordered identities / 534 phases. SHA-256:
`efc06a5a3540d82ad16239aceb54521b383931187ba124b9ce392e9d2a1d90ee`.
Independent review verified every content/tool/nested hash, all ordered cases
and phases, the prior 994 unchanged content files, repeated artifacts, decoded
callback ABI and whole-28 inventory/refusals. No blocking source defect was
found within the documented prepublication/disjoint-output/lifetime contracts.
Launcher: `/tmp/whip-discovery-read-proof.K8flA0/run.py`, fresh output paths
required. `executed-proof/` holds the exact test-run objects, artificial ELFs,
stack reports, repeated builds and strict-link refusals. Absolute nested
fixture references point to the retained temporary run; copies are also hashed.

The startup test composes service registration, both common callback roots,
advertising builder and resubmission in one persistent ARM context. Both call
orders are test fixtures; skipped boot, MAC acquisition, ROM/RTOS, registration
results and callback ownership are not physical evidence. Its 38-byte stock
edit spans exist only in the emulator; preserved IDs and stored callback
pointers survive the advertising paths. It does not prove UART traffic or
full boot continuity. An independent read-only review found no blocking defect.

The separately linked actual six-argument read callback uses the real encoder
and runtime with artificial identity/ID storage. Invalid reads clear outputs;
successful reads borrow the immutable 20-byte value and leave Health/no-action
unchanged. No production write/CCCD callback or callback table is supplied.
Real boot-ID generation, initialization, ownership and stack lifetime remain
required. No initialized flag or fake production identity hides those gaps.

Whole 28-object input lower bound: 10798/9520, at least 1278 over before
callback constant12, alignment/final unwind and remaining bindings. Exactly six
strong symbols remain unresolved; no production ELF. Identity20 is already
planned and is not charged twice. Artificial ELFs are not installable images.
See [the audit](../../../docs/UNIFIED_WECHAT_RETIREMENT.md) and
[read contract](../../../docs/UNIFIED_GATT_READ_BINDING.md).
