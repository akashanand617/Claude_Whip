# Stack plan stage 1 (read-only)

198/198 CD01, verified disconnect. `pxCurrentTCB`, 256 B of ROM kernel lists and the MSP paint (hash-only). Read-only; nothing followed. The MSP paint is fully intact (depth ≤ 1152 B). Combined results: `../ram-ownership-stack-watermarks/README.md`.
