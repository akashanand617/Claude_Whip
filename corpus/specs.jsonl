{"id": "scope-02", "dimension": "scope", "status": "spec", "domain": "cpp-daemon", "task": "Fix the unchecked return value of write() in the BLE send path.", "a_pole": "minimal", "a_spec": "Add the one missing check with the file's existing error style; touch nothing else.", "b_pole": "opportunistic", "b_spec": "Same check, and also fix the two adjacent unchecked calls in the same function and align their log messages."}
{"id": "scope-03", "dimension": "scope", "status": "spec", "domain": "web-backend", "task": "Return 404 instead of 500 when the invoice id doesn't exist.", "a_pole": "minimal", "a_spec": "Change the one handler named in the request.", "b_pole": "opportunistic", "b_spec": "Same change, applied also to the three sibling handlers with the identical flaw, noted in the reply."}
{"id": "scope-04", "dimension": "scope", "status": "spec", "domain": "ml-training", "task": "Bump the checkpoint format version now that optimizer state is saved.", "a_pole": "minimal", "a_spec": "Bump the constant and add the load-shim for the old version.", "b_pole": "opportunistic", "b_spec": "Same, plus refactor the accumulated if/elif version handling into a version->loader table while in the file."}
{"id": "scope-05", "dimension": "scope", "status": "spec", "domain": "scripting", "task": "Make the backup shell script skip symlinks.", "a_pole": "minimal", "a_spec": "Add the single -type f condition (or equivalent) and nothing else.", "b_pole": "opportunistic", "b_spec": "Same fix, plus quote the three unquoted path variables encountered while editing. Careful: both variants must behave identically on the backup itself."}
{"id": "verbosity-02", "dimension": "verbosity", "status": "spec", "domain": "cpp-daemon", "task": "Why did this iterator into a std::vector become invalid after push_back?", "a_pole": "terse", "a_spec": "Two sentences: reallocation invalidates iterators; index or reserve. Show the one-line fix.", "b_pole": "explanatory", "b_spec": "Same fix, preceded by growth/reallocation semantics, when invalidation does and doesn't occur, and the reserve() alternative."}
{"id": "verbosity-03", "dimension": "verbosity", "status": "spec", "domain": "sql", "task": "Why is this LEFT JOIN dropping rows? (a WHERE clause filters a right-table column)", "a_pole": "terse", "a_spec": "One sentence: the WHERE turns it into an inner join; move the predicate into ON. Show the moved line.", "b_pole": "explanatory", "b_spec": "Same fix, with the join-then-filter evaluation order explained and a two-row worked example showing the NULL row disappearing."}
{"id": "verbosity-04", "dimension": "verbosity", "status": "spec", "domain": "embedded", "task": "Why does the ISR miss ticks when debug logging is enabled?", "a_pole": "terse", "a_spec": "Answer (logging in the ISR exceeds the tick budget) plus the fix (set a flag, log in the main loop).", "b_pole": "explanatory", "b_spec": "Same answer and fix, with interrupt latency arithmetic: cycles per log call vs cycles per tick, and why UART writes block."}
{"id": "verbosity-05", "dimension": "verbosity", "status": "spec", "domain": "python-tooling", "task": "What is the `if __name__ == \"__main__\"` guard at the bottom of this script for?", "a_pole": "terse", "a_spec": "One sentence: it runs only when executed directly, not on import.", "b_pole": "explanatory", "b_spec": "Same answer, plus how Python sets __name__ during import vs execution and what breaks without the guard (side effects on import, multiprocessing on Windows)."}
{"id": "comments-02", "dimension": "comments", "status": "spec", "domain": "cpp-daemon", "task": "Implement the ring-buffer write with wraparound for the sample queue.", "a_pole": "sparse", "a_spec": "Identical code; one comment stating the power-of-two-capacity masking invariant.", "b_pole": "narrated", "b_spec": "Identical code; comment each step (advance head, mask, detect overwrite), including the invariant comment from A."}
{"id": "comments-03", "dimension": "comments", "status": "spec", "domain": "scripting", "task": "Write the nightly cleanup script: delete captures older than 30 days, keep the newest 5 regardless.", "a_pole": "sparse", "a_spec": "One comment on the keep-newest-5 override, since it deliberately contradicts the age rule.", "b_pole": "narrated", "b_spec": "Same script; each pipeline stage commented (list, sort, slice, delete), including A's override comment."}
{"id": "comments-04", "dimension": "comments", "status": "spec", "domain": "ml-training", "task": "Add linear LR warmup over the first N steps to the scheduler.", "a_pole": "sparse", "a_spec": "One comment on the step+1 offset that avoids a zero LR at step 0.", "b_pole": "narrated", "b_spec": "Same code; narrate the ramp formula, the clamp, and the handoff to the base schedule, including A's offset comment."}
{"id": "comments-05", "dimension": "comments", "status": "spec", "domain": "embedded", "task": "Implement the packet checksum (sum of bytes mod 256) for outgoing frames.", "a_pole": "sparse", "a_spec": "No comments; the code is three lines and self-evident. (Sparse may mean zero when nothing is non-obvious.)", "b_pole": "narrated", "b_spec": "Same three lines with a comment per line and a header comment restating the algorithm."}
{"id": "abstraction-02", "dimension": "abstraction", "status": "spec", "domain": "web-backend", "task": "Two list endpoints duplicate the same pagination boilerplate; unify them.", "a_pole": "concrete", "a_spec": "One 8-line paginate(query, page, size) helper used by both endpoints.", "b_pole": "generalized", "b_spec": "A Paginator class with pluggable page-size policy and cursor support stubbed for future endpoints; both endpoints use it."}
{"id": "abstraction-03", "dimension": "abstraction", "status": "spec", "domain": "cpp-daemon", "task": "Parse the two accelerometer packet subtypes we actually receive.", "a_pole": "concrete", "a_spec": "Two small parse functions, one per subtype, dispatched by a switch.", "b_pole": "generalized", "b_spec": "A table-driven parser registry mapping subtype byte to handler, sized for the other 13 subtypes if they're ever needed."}
{"id": "abstraction-04", "dimension": "abstraction", "status": "spec", "domain": "scripting", "task": "The report needs both a plain-text and a CSV rendering of the same rows.", "a_pole": "concrete", "a_spec": "Two functions, render_text(rows) and render_csv(rows).", "b_pole": "generalized", "b_spec": "A Renderer protocol with TextRenderer and CsvRenderer classes and a get_renderer(fmt) factory."}
{"id": "abstraction-05", "dimension": "abstraction", "status": "spec", "domain": "ml-training", "task": "Both trainers duplicate metric logging; consolidate.", "a_pole": "concrete", "a_spec": "One shared log_metrics(step, metrics) function writing the jsonl line both trainers already write.", "b_pole": "generalized", "b_spec": "A MetricsSink interface with JsonlSink now and a stub for a future TensorBoard sink; both trainers take a sink."}
{"id": "defensiveness-02", "dimension": "defensiveness", "status": "spec", "domain": "cpp-daemon", "task": "Implement parse_packet(const uint8_t*, size_t) for the 16-byte UART frame.", "a_pole": "trusting", "a_spec": "Assert the length, decode fields directly; malformed input is a programming error upstream.", "b_pole": "defensive", "b_spec": "Return an error enum for wrong length and bad checksum, decode only after validation, no asserts."}
{"id": "defensiveness-03", "dimension": "defensiveness", "status": "spec", "domain": "web-backend", "task": "Add the internal /admin/requeue endpoint taking job_id and priority query params.", "a_pole": "trusting", "a_spec": "Read the params, requeue; internal tooling, garbage in crashes with a 500 and a traceback in the log.", "b_pole": "defensive", "b_spec": "Validate presence, type, and priority range; return 400s with specific messages; clamp priority."}
{"id": "defensiveness-04", "dimension": "defensiveness", "status": "spec", "domain": "scripting", "task": "Write the deploy script reading TARGET_HOST, RELEASE_TAG, DEPLOY_KEY from the environment.", "a_pole": "trusting", "a_spec": "set -u and use the variables; an unset var aborts with the shell's own message.", "b_pole": "defensive", "b_spec": "Check each variable explicitly up front and print which one is missing and where it's documented."}
{"id": "defensiveness-05", "dimension": "defensiveness", "status": "spec", "domain": "ml-training", "task": "Write the loader for our internal .npz capture archives.", "a_pole": "trusting", "a_spec": "Index the expected keys; a missing key raises KeyError with the archive path in the traceback.", "b_pole": "defensive", "b_spec": "Verify keys, shapes, and dtype before returning, raising one message naming file, missing key, and expected shape."}
{"id": "dependencies-02", "dimension": "dependencies", "status": "spec", "domain": "python-cli", "task": "Print the per-session summary as an aligned table in the terminal.", "a_pole": "handroll", "a_spec": "~10 lines computing column widths with max(len(...)) and printing with str.ljust.", "b_pole": "library", "b_spec": "Use tabulate (or rich.table) and note the added dependency."}
{"id": "dependencies-03", "dimension": "dependencies", "status": "spec", "domain": "python-backend", "task": "Parse ISO-8601 durations like PT1H30M from the partner API.", "a_pole": "handroll", "a_spec": "A ~12-line regex parser covering the H/M/S forms the partner actually sends.", "b_pole": "library", "b_spec": "Add isodate and call parse_duration, covering the full standard including week and fractional forms."}
{"id": "dependencies-04", "dimension": "dependencies", "status": "spec", "domain": "cpp-daemon", "task": "The daemon needs command-line flags: --device, --rate, --verbose.", "a_pole": "handroll", "a_spec": "A ~20-line loop over argv with a usage() function; no new dependency in the embedded build.", "b_pole": "library", "b_spec": "Vendor the header-only CLI11 and declare the three options; help text and validation come free."}
{"id": "dependencies-05", "dimension": "dependencies", "status": "spec", "domain": "ml-training", "task": "Show progress during the multi-hour training loop.", "a_pole": "handroll", "a_spec": "Manual carriage-return print of step, loss, and ETA every N steps.", "b_pole": "library", "b_spec": "Wrap the loader in tqdm; rate and ETA come free, and it degrades in non-TTY logs."}
{"id": "initiative-02", "dimension": "initiative", "status": "spec", "domain": "python-tooling", "task": "Delete the stale captures in data/raw.", "a_pole": "ask-first", "a_spec": "Ask what counts as stale (age? superseded firmware? referenced by no analysis?) before deleting anything.", "b_pole": "act-first", "b_spec": "Choose >30 days AND not referenced by any analysis output, print the would-delete list, delete, and report the criterion used and how to restore from trash."}
{"id": "initiative-03", "dimension": "initiative", "status": "spec", "domain": "cpp-daemon", "task": "Make the daemon reconnect automatically when the ring drops.", "a_pole": "ask-first", "a_spec": "Ask the two policy questions that change the design: give up ever? and is a reconnect storm during firmware flashing acceptable?", "b_pole": "act-first", "b_spec": "Implement exponential backoff capped at 60 s, never give up, suspend during DFU; state all three choices as reversible assumptions."}
{"id": "initiative-04", "dimension": "initiative", "status": "spec", "domain": "ml-training", "task": "Train with the new augmentation and tell me if it helps.", "a_pole": "ask-first", "a_spec": "Ask which config and how many seeds constitute an answer before spending GPU hours.", "b_pole": "act-first", "b_spec": "Run the default config at 3 seeds, report means and spread against baseline, note that more seeds are queued if the gap looks marginal."}
{"id": "initiative-05", "dimension": "initiative", "status": "spec", "domain": "sql", "task": "Add an index to speed up the slow invoice-search query.", "a_pole": "ask-first", "a_spec": "Ask about write volume on the table before adding, since the covering index taxes every insert.", "b_pole": "act-first", "b_spec": "Add the covering index CONCURRENTLY, report the query-plan change, and note the insert overhead measured on staging with rollback DDL included."}
{"id": "proactivity-02", "dimension": "proactivity", "status": "spec", "domain": "cpp-daemon", "task": "Add a clamp(value, lo, hi) helper to util.h.", "a_pole": "exact", "a_spec": "The function, matching the header's existing style. Nothing else.", "b_pole": "collateral", "b_spec": "The identical function plus a unit test with boundary cases and a doc comment; both mentioned in the reply."}
{"id": "proactivity-03", "dimension": "proactivity", "status": "spec", "domain": "web-backend", "task": "Add created_at to the invoice list response.", "a_pole": "exact", "a_spec": "Add the field to the serializer. Done.", "b_pole": "collateral", "b_spec": "Same field, plus the API doc entry and a serializer test asserting the ISO format."}
{"id": "proactivity-04", "dimension": "proactivity", "status": "spec", "domain": "scripting", "task": "Write a script that rotates the daemon logs, keeping 7 days.", "a_pole": "exact", "a_spec": "The script.", "b_pole": "collateral", "b_spec": "The identical script plus the crontab line to install it and a README section, both unrequested."}
{"id": "proactivity-05", "dimension": "proactivity", "status": "spec", "domain": "ml-training", "task": "Add a --grad-clip flag to the training script.", "a_pole": "exact", "a_spec": "The flag wired to clip_grad_norm_. Nothing else.", "b_pole": "collateral", "b_spec": "Same flag, plus a test that gradients exceed-then-clip, and the default documented in the config reference."}
{"id": "idiom-02", "dimension": "idiom", "status": "spec", "domain": "python-tooling", "task": "Add a load_manifest() function to this legacy module that uses % formatting and os.path throughout.", "a_pole": "conform", "a_spec": "os.path.join and % formatting, matching every neighbor function.", "b_pole": "modernize", "b_spec": "pathlib and f-strings, with a note that the function introduces the file's first modern idioms."}
{"id": "idiom-03", "dimension": "idiom", "status": "spec", "domain": "web-backend", "task": "Add the retry wrapper in this JS service written entirely in .then() promise chains.", "a_pole": "conform", "a_spec": "A .then/.catch chain matching the file.", "b_pole": "modernize", "b_spec": "async/await with try/catch, noting the style seam it introduces."}
{"id": "idiom-04", "dimension": "idiom", "status": "spec", "domain": "ml-training", "task": "Add the evaluation config to this repo where every script passes bare argparse Namespaces around.", "a_pole": "conform", "a_spec": "Extend the Namespace pattern exactly as the other six scripts do.", "b_pole": "modernize", "b_spec": "A typed dataclass config for the new script, converted from argparse at the edge, noted as a divergence."}
{"id": "idiom-05", "dimension": "idiom", "status": "spec", "domain": "sql", "task": "Add the refunds table to a schema that abbreviates column names (amt_cents, cust_id, crtd_at).", "a_pole": "conform", "a_spec": "Follow the abbreviations: rfnd_amt_cents, cust_id, crtd_at.", "b_pole": "modernize", "b_spec": "Full names (refund_amount_cents, customer_id, created_at), noting the naming seam against the sibling tables."}
{"id": "hedging-02", "dimension": "hedging", "status": "spec", "domain": "python-backend", "task": "Is the memory leak fixed? (patch deployed, RSS flat for 24h where it climbed 200MB/day before)", "a_pole": "committed", "a_spec": "Yes — flat where it climbed, the leak site is identified and removed, the mechanism explains the old slope.", "b_pole": "cautious", "b_spec": "Almost certainly, with the same evidence, plus: 24h is one traffic cycle, recommend watching through the weekly batch job before closing."}
{"id": "hedging-03", "dimension": "hedging", "status": "spec", "domain": "cpp-daemon", "task": "What caused the crash? (ASAN shows use-after-free of the session object in the reconnect path)", "a_pole": "committed", "a_spec": "State the cause and the fix; the ASAN trace plus a reproducing test settles it.", "b_pole": "cautious", "b_spec": "Same trace and fix, framed as the most likely of the freed-object's three access paths, with the other two listed for review."}
{"id": "hedging-04", "dimension": "hedging", "status": "spec", "domain": "embedded", "task": "Why do BLE drops spike when I walk away from the desk? (loss 10.9% walking vs 0.24% seated)", "a_pole": "committed", "a_spec": "Distance and body attenuation — the numbers track proximity, not motion; state it plainly with the two measurements.", "b_pole": "cautious", "b_spec": "Same measurements, framed as consistent with attenuation, alternatives (interference zones, antenna orientation) each named with a discriminating check."}
{"id": "hedging-05", "dimension": "hedging", "status": "spec", "domain": "devops", "task": "Why is the integration test flaky on CI but never locally? (two jobs bind the same fixed port)", "a_pole": "committed", "a_spec": "Port collision under parallel jobs; the failure log shows EADDRINUSE; fix is an ephemeral port. Stated as settled.", "b_pole": "cautious", "b_spec": "Same finding, presented as the leading explanation with a suggested week of post-fix observation and one alternative (teardown race) noted."}
{"id": "cleverness-02", "dimension": "cleverness", "status": "spec", "domain": "python-backend", "task": "Build the tag -> [job ids] index from the list of jobs.", "a_pole": "explicit", "a_spec": "Nested for loops with named intermediates and an explicit membership check.", "b_pole": "dense", "b_spec": "defaultdict with a two-line loop, mentioning the equivalent comprehension form."}
{"id": "cleverness-03", "dimension": "cleverness", "status": "spec", "domain": "cpp-daemon", "task": "Find the first packet whose amplitude exceeds the threshold.", "a_pole": "explicit", "a_spec": "An indexed for loop with an early return.", "b_pole": "dense", "b_spec": "std::find_if with a lambda, one statement."}
{"id": "cleverness-04", "dimension": "cleverness", "status": "spec", "domain": "scripting", "task": "Extract the unique client IPs from the access log.", "a_pole": "explicit", "a_spec": "A small Python loop: split, collect in a set, sorted print.", "b_pole": "dense", "b_spec": "One shell pipeline: awk '{print $1}' | sort -u."}
{"id": "cleverness-05", "dimension": "cleverness", "status": "spec", "domain": "sql", "task": "Get each customer's most recent invoice.", "a_pole": "explicit", "a_spec": "A join against a GROUP BY max(created_at) subquery, each step aliased and readable.", "b_pole": "dense", "b_spec": "ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY created_at DESC) filtered to 1."}
{"id": "testing-02", "dimension": "testing", "status": "spec", "domain": "cpp-daemon", "task": "Write tests for crc16_modbus() used in DFU framing.", "a_pole": "focused", "a_spec": "Three known-answer vectors from the spec, including the empty payload.", "b_pole": "exhaustive", "b_spec": "Known vectors plus single-byte sweep, all-0xFF frame, incremental-equals-oneshot property, and a mutation check (one flipped bit changes the CRC)."}
{"id": "testing-03", "dimension": "testing", "status": "spec", "domain": "python-tooling", "task": "Write tests for parse_duration ('1h30m' -> 5400).", "a_pole": "focused", "a_spec": "Four cases: composite, single unit, zero, and one malformed input.", "b_pole": "exhaustive", "b_spec": "Parameterized grid over unit combinations and orderings, whitespace variants, overflow, and a table of malformed inputs each asserting the error message."}
{"id": "testing-04", "dimension": "testing", "status": "spec", "domain": "web-backend", "task": "Write tests for the paginated /invoices endpoint.", "a_pole": "focused", "a_spec": "Happy path with two pages, and the empty-result case.", "b_pole": "exhaustive", "b_spec": "Those plus last-page remainder, page past the end, size=0, negative page, size over the cap, and stable ordering across pages."}
{"id": "testing-05", "dimension": "testing", "status": "spec", "domain": "ml-training", "task": "Write tests for the windowing function (window 50, stride 6).", "a_pole": "focused", "a_spec": "Output shape for a nominal length, and one remainder case at the tail.", "b_pole": "exhaustive", "b_spec": "Shape grid across lengths including shorter-than-window, overlap-content property between consecutive windows, dtype preservation, and the empty input."}
